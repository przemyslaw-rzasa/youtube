import * as React from "react";

import TextField from "@material-ui/core/TextField";

export const SearchBar = (props: any) => {
  return <TextField fullWidth placeholder="Search for a video" />;
};
