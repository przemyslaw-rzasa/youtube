import { join } from "path";

export const PUBLIC_PATH = join(process.cwd(), "public");
export const PUBLIC_VIDEOS_PATH = join(PUBLIC_PATH, "videos");
export const PUBLIC_IMAGES_PATH = join(PUBLIC_PATH, "images");
